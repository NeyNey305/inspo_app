<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!-- DIV FOR NEW USER FORM  -->

    <div class="field">
      <%= f.label :email %><br />
      <%= f.text_field :email, class:"new-user-form" %>
    </div>

    <div class="field">
      <%= f.label :password %><br />
      <%= f.password_field :password, class:"new-user-form" %>
    </div>
    <div class="field">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, class:"new-user-form" %>
    </div>

    <div class="actions">
      <%= f.submit class:"btn btn-success" %>
    </div>


  <% end %>
